# 国内镜像：解决 Electron 二进制从 GitHub 下载被墙的问题（TypeError: fetch failed）
# @electron/get 会读取 electron_mirror 配置，改从 npmmirror 下载。
electron_mirror=https://npmmirror.com/mirrors/electron/
# 可选：npm 包也走国内镜像（如需可取消注释）
# registry=https://registry.npmmirror.com/
